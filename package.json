{
  "name": "learn-claude-code-1",
  "private": true,
  "scripts": {
    "postinstall": "npm --prefix web ci",
    "dev": "npm --prefix web run dev",
    "build": "npm --prefix web run build",
    "start": "npm --prefix web run start",
    "extract": "npm --prefix web run extract"
  }
}
